# Copy to .env.local. The game runs WITHOUT a key using local fallbacks
# (hashed-embedding RAG + rubric evaluator). Add a key to enable the LLM paths.
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
